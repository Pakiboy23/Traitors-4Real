# cloudbuild.yaml
options:
  logging: CLOUD_LOGGING_ONLY  # or NONE, or specify build_logs_bucket: gs://your-bucket

steps:
  - name: 'gcr.io/cloud-builders/npm'
    args: ['install']
  # ... other steps ...
