# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.next
dist
dist-ssr
*.local
*.broken

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/.env


backups/
.env.local
.vercel

# Next.js static export + Capacitor bundled web assets
/out
/native-web

# TypeScript incremental build cache
*.tsbuildinfo

# Working dir if capture is pointed at the repo root. Committed App Store
# shots live in store/screenshots/ and are not ignored.
/screenshots/

# Claude Code session memory (local only)
/MEMORY.md

# Next.js generated — regenerates differently after `next dev` vs `next build`
next-env.d.ts
